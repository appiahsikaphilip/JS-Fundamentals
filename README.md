# Project Title

A brief description of what this project does and who it's for.

## Technical Requirements

To maintain modern coding standards and ensure successful execution, this project adheres to the following rules:

* **Variable Declaration:** Only `let` and `const` are permitted. The use of `var` is strictly prohibited.
* **Output:** All feedback and results must be handled via `console.log()`.
* **Documentation:** A non-empty `README.md` must be present in the root directory.

## Installation

Install the project dependencies using npm:

```bash
npm install
